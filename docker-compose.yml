services:
  postgres:
    image: postgres:17
    container_name: salesland-postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: salesland_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: